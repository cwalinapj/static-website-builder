{
  "name": "static-builder-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "build": "node --check index.js && node --check cloudflare.js && node --check tokenStore.js && node --check crypto.js",
    "lint": "npm run build",
    "test": "node -e \"console.log('No server tests configured')\""
  }
}
